/** 
  * Módulo para realizar cálculos matemáticos 
  */ 
 /**
  * Calcula la diferencia absoluta entre dos temperaturas
  */
 export const getTempDifference = (temp1, temp2) => {
     return Math.abs(temp1 - temp2);
 };
